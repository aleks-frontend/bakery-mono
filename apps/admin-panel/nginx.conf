server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback so react-router-dom's client-side routes survive a
    # hard refresh / direct link instead of 404ing at nginx.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
